name: reg
description: Extended event registry.

note:
  Has been observed on target IDs 1 and 2. 

commands:
  enable: reg/event/enable
  disable: reg/event/disable

properties:
  per-instance: true
